import { ConvertGpaPipe } from "./convert-gpa.pipe";

describe("ConvertGpaPipe", () => {
  it("create an instance", () => {
    const pipe = new ConvertGpaPipe();
    expect(pipe).toBeTruthy();
  });
});
